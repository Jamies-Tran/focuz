spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: administration-service
              uri: lb://ADMINISTRATION-SERVICE
              predicates:
                - Path=/api/administration-service/**
              filters:
                - RewritePath=/api/administration-service/(?<segment>.*),/${segment}
  application:
    name: gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka